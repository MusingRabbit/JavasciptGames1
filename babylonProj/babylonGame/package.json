{
  "name": "testproject",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "buildLighting01" : "tsc && vite build lighting01",
    "previewLightint01" : "vite preview lighting01 --host",
    "buildVillage" : "tsc && vite build village",
    "previewVillage" : "vite preview village --host"
    
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.9"
  }
}
